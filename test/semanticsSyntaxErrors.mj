program MaxCifra
int a, c, maxcif;
    char e = '2';              //sintaksna greska oporavak do ;
    int c;                     //semanticka, c je vec definisano
{
    void nekaFunc() char a; int z;{
        read(a);
        maxcif = 0;
        c = a % 10;
        maxcif = c;
        a = a/10;             //semanticka, operandi uz / moraju biti int
        read(x);                 //semanticka, x nije deklarisan
        x++;                     //semanticka, isto
        print(max);              //semanticka, isto
    }

    void main() int ok;{
        ok = 9;
    }
}